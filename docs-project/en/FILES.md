# 📁 项目文件说明

本文档列出项目中所有重要文件及其作用。

---

## 📚 文档文件

| 文件 | 说明 | 用途 |
|------|------|------|
| [README.md](README.md) | 主文档 | 完整的开发和部署指南 |
| [DEPLOYMENT.md](DEPLOYMENT.md) | 部署文档 | GitHub Pages 快速部署步骤 |
| [COMMANDS.md](COMMANDS.md) | 命令速查 | 常用命令快速参考 |
| [CHECKLIST.md](CHECKLIST.md) | 检查清单 | 部署前检查列表 |
| [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) | 项目总结 | 重构说明和技术细节 |
| [FILES.md](FILES.md) | 本文档 | 文件说明 |

---

## 🔧 配置文件

| 文件 | 说明 | 作用 |
|------|------|------|
| `requirements.txt` | Python 依赖 | 项目所需的 Python 包 |
| `.gitignore` | Git 忽略规则 | 指定不提交到 Git 的文件 |
| `manage.py` | Django 管理脚本 | 执行 Django 命令的入口 |

---

## 🎯 核心代码

### Django 项目配置

```
RunProject/
├── __init__.py
├── settings.py          # 项目设置 (数据库、模板、静态文件等)
├── urls.py              # URL 路由配置
├── wsgi.py              # WSGI 部署接口
└── asgi.py              # ASGI 部署接口
```

### 应用代码 (app/)

```
app/
├── __init__.py
├── models.py            # 数据模型 (Article, Tag, Comment, BoardMessage)
├── views.py             # 视图函数 (首页、列表、详情等)
├── admin.py             # Django 后台配置
├── apps.py              # 应用配置 (注册信号)
├── signals.py           # 🆕 信号处理 (自动生成静态站点)
├── urls.py              # URL 路由
├── api_views.py         # API 视图
└── tests.py             # 测试文件
```

### 静态生成命令

```
app/management/
├── __init__.py
└── commands/
    ├── __init__.py
    └── generate_static_site.py    # 🆕 静态站点生成核心逻辑
```

**作用**: 将 Django 动态站点渲染为静态 HTML 到 `docs/` 目录

**使用**: `python manage.py generate_static_site`

---

## 🎨 前端文件

### Django 模板 (templates/)

| 文件 | 说明 | 页面内容 |
|------|------|----------|
| `base.html` | 基础模板 | 导航栏、页面框架 |
| `index.html` | 首页模板 | 博客首页 |
| `list.html` | 列表模板 | 文章列表页 (支持分页和标签筛选) |
| `detail.html` | 详情模板 | 文章详情页 (含评论) |
| `about.html` | 关于页面 | 关于作者 |
| `board.html` | 留言板 | 留言板页面 |

**特性**: 所有模板已支持静态和动态双模式 (`is_static` 参数)

### 静态资源 (static/)

```
static/
├── css/                 # 样式文件
├── js/                  # JavaScript 文件
└── images/              # 图片资源
```

### 用户上传 (media/)

```
media/
├── covers/              # 文章封面 (自动水印)
│   └── 2024/
│       └── 11/
└── avatars/             # 用户头像
    └── icon_*.png       # 自动生成的头像池
```

**注意**: `media/` 不提交到 Git,但会复制到 `docs/media/`

---

## 🚀 部署相关

### 脚本文件

| 文件 | 说明 | 用途 |
|------|------|------|
| `deploy.sh` | 🆕 一键部署脚本 | 自动化生成和推送流程 |
| `test_static_generation.sh` | 🆕 测试脚本 | 测试静态生成功能 |
| `scripts/dev.sh` | 开发脚本 | 一键启动开发环境 |

**使用**:
```bash
# 一键部署
bash deploy.sh

# 测试生成
bash test_static_generation.sh
```

### 静态站点输出 (docs/)

```
docs/                    # ⭐ GitHub Pages 发布目录
├── index.html           # 首页
├── list.html            # 文章列表第 1 页
├── list_page_2.html     # 文章列表第 2 页
├── list_tag_*.html      # 标签筛选页
├── article_*.html       # 文章详情页
├── about.html           # 关于页面
├── board.html           # 留言板
├── static/              # 静态资源副本
├── media/               # 媒体文件副本
├── sitemap.xml          # 站点地图
└── .nojekyll            # GitHub Pages 配置
```

**重要**: `docs/` 目录必须提交到 Git 用于 GitHub Pages 部署

---

## 🗄️ 数据文件

| 文件 | 说明 | 是否提交 Git |
|------|------|--------------|
| `db.sqlite3` | SQLite 数据库 | ❌ 不提交 |
| `backup.json` | 数据备份文件 | ⚠️ 可选 |

**数据库管理**:
```bash
# 备份
python manage.py dumpdata > backup.json

# 恢复
python manage.py loaddata backup.json
```

---

## 🧩 Vue 前端 (frontend/)

```
frontend/
├── src/                 # Vue 源码
│   ├── components/      # 组件
│   ├── views/           # 页面
│   └── main.ts          # 入口文件
├── dist/                # 构建输出
├── package.json         # npm 依赖
├── vite.config.ts       # Vite 配置
└── tsconfig.json        # TypeScript 配置
```

**说明**: Vue 前端为独立科技风客户端,暂未集成到静态生成流程

---

## 📦 Python 包缓存

```
__pycache__/             # Python 字节码缓存
*.pyc                    # 编译后的 Python 文件
```

**说明**: 自动生成,已在 `.gitignore` 排除

---

## 🔍 文件优先级

### 必须提交到 Git

✅ 所有 Python 代码 (`*.py`)
✅ 所有模板文件 (`templates/**`)
✅ 静态资源 (`static/**`)
✅ **docs/ 目录** (GitHub Pages 发布源)
✅ 配置文件 (`requirements.txt`, `.gitignore`)
✅ 文档文件 (`*.md`)
✅ 脚本文件 (`*.sh`)

### 不要提交到 Git

❌ `db.sqlite3` (数据库)
❌ `media/` (用户上传文件)
❌ `__pycache__/` (Python 缓存)
❌ `*.pyc` (字节码)
❌ `frontend/node_modules/` (npm 依赖)
❌ `.DS_Store` (macOS 系统文件)

### 可选提交

⚠️ `backup.json` (数据备份) - 如需在多台电脑同步数据
⚠️ `frontend/dist/` (Vue 构建输出) - 如集成到静态生成

---

## 🗂️ 目录结构总览

```
RunProject/
│
├── 📚 文档
│   ├── README.md
│   ├── DEPLOYMENT.md
│   ├── COMMANDS.md
│   ├── CHECKLIST.md
│   ├── PROJECT_SUMMARY.md
│   └── FILES.md
│
├── 🔧 配置
│   ├── .gitignore
│   ├── requirements.txt
│   └── manage.py
│
├── 🎯 Django 项目
│   ├── RunProject/
│   │   ├── settings.py
│   │   └── urls.py
│   └── app/
│       ├── models.py
│       ├── views.py
│       ├── admin.py
│       ├── signals.py              # 🆕
│       └── management/
│           └── commands/
│               └── generate_static_site.py  # 🆕
│
├── 🎨 前端
│   ├── templates/
│   │   ├── base.html              # ✏️ 已修改
│   │   ├── list.html              # ✏️ 已修改
│   │   └── detail.html            # ✏️ 已修改
│   ├── static/
│   └── frontend/                  # Vue 前端
│
├── 🚀 部署
│   ├── deploy.sh                  # 🆕
│   ├── test_static_generation.sh  # 🆕
│   └── docs/                      # 🆕 GitHub Pages 输出
│
└── 🗄️ 数据
    ├── db.sqlite3                 # ❌ 不提交
    └── media/                     # ❌ 不提交
```

---

## 📝 文件编辑优先级

### 日常修改 (高频)

1. **创建文章**: Django 后台操作 (无需编辑代码)
2. **调整样式**: `templates/base.html` 或 `static/css/`
3. **修改文案**: `templates/*.html`

### 进阶修改 (中频)

1. **添加功能**: `app/views.py`, `app/models.py`
2. **修改路由**: `app/urls.py`
3. **后台定制**: `app/admin.py`

### 系统级修改 (低频)

1. **项目配置**: `RunProject/settings.py`
2. **静态生成逻辑**: `app/management/commands/generate_static_site.py`
3. **信号处理**: `app/signals.py`

---

## 🎓 学习建议

**新手建议阅读顺序**:
1. [README.md](README.md) - 了解整体流程
2. [DEPLOYMENT.md](DEPLOYMENT.md) - 学习部署步骤
3. [CHECKLIST.md](CHECKLIST.md) - 按清单操作
4. [COMMANDS.md](COMMANDS.md) - 查阅常用命令
5. 本文档 - 了解文件结构

**进阶开发建议**:
1. 阅读 [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) 了解技术实现
2. 研究 `app/management/commands/generate_static_site.py`
3. 学习 `app/signals.py` 的自动触发机制
4. 实验修改模板和样式

---

**💡 提示**: 善用 `tree` 命令查看目录结构:
```bash
tree -L 2 -I '__pycache__|node_modules|dist'
```
